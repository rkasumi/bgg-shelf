{
  "name": "bgg-shelf",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check:secrets": "bash scripts/check-secrets.sh",
    "validate:sample": "node scripts/validate-sample.mjs"
  }
}
